Enssure all libraries are isntalled using `pip install -r requirements.txt` at main file directory

While in streamlit folder:
To run streamlit, use `streamlit run main.py`

to edit tab, put your code under `with tab2:`, `with tab3:` respectively
